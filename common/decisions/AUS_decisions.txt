AUS_WARPLAN = {
	AUS_WARPLAN_PRE = {
		icon = generic_prepare_civil_war
		available = {
			tag = AUS
			has_war = no
			has_completed_focus = war_PLAN
		}
		

		fire_only_once = yes
		cost = 0
		
		days_remove = 5
		days_re_enable = 5

		ai_will_do = {
			factor = 100
			#modifier = {
			#	factor = X

			#}
		}

		remove_effect = {
			AUS = {
				declare_war_on = { target = MEK type = take_state_focus }
				declare_war_on = { target = SAX type = take_state_focus }
				declare_war_on = { target = PRE type = take_state_focus }
			}
		}

	}
	AUS_WARPLAN_DEN = {
		icon = generic_prepare_civil_war
		available = {
			tag = AUS
			has_war = no
			NOT = { has_war_with = PRE}
			owns_state = 64
		}
		

		fire_only_once = yes
		cost = 0
		
		days_remove = 5
		days_re_enable = 5

		ai_will_do = {
			factor = 100
			#modifier = {
			#	factor = X

			#}
		}

		remove_effect = {
			AUS = {
				declare_war_on = { target = DEN type = take_state_focus }
			}
		}

	}
	AUS_WARPLAN_DEN_END = {
		icon = generic_prepare_civil_war
		available = {
			tag = AUS
			controls_state = 37
			has_war = yes
		}
		

		fire_only_once = yes
		cost = 0
		
		days_remove = 5
		days_re_enable = 5

		ai_will_do = {
			factor = 100
			#modifier = {
			#	factor = X

			#}
		}

		remove_effect = {
			AUS = {
				white_peace = DEN
				set_autonomy = {
					target = DEN
					autonomy_state = autonomy_colony
				}
				transfer_state = 58
				transfer_state = 912
				set_country_flag = complete_DEN
			}
		}

	}
	AUS_WARPLAN_EST = {
		icon = generic_prepare_civil_war
		available = {
			tag = AUS
			has_country_flag = complete_DEN
		}
		

		fire_only_once = yes
		cost = 0
		
		days_remove = 5
		days_re_enable = 5

		ai_will_do = {
			factor = 100
			#modifier = {
			#	factor = X

			#}
		}

		remove_effect = {
			AUS = {
				white_peace = EST
				set_autonomy = {
					target = EST
					autonomy_state = autonomy_colony
				}
				transfer_state = 811
				transfer_state = 912
				
			}
		}

	}
	AUS_WARPLAN_DON = {
		icon = generic_prepare_civil_war
		available = {
			tag = AUS
			has_country_flag = complete_DEN
			AUS = {
				is_in_faction_with = LON
			}
		}
		

		fire_only_once = yes
		cost = 0
		
		days_remove = 5
		days_re_enable = 5

		ai_will_do = {
			factor = 100
			#modifier = {
			#	factor = X

			#}
		}

		remove_effect = {
			AUS = {
				white_peace = DON
			}
			LON = {
				add_to_faction = DON
			}
			DON = {
				set_politics = { ruling_party = neutrality }
			}
		}

	}

}
